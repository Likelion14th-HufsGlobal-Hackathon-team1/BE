spring:
  datasource:
    url: jdbc:h2:mem:mcm_archiv;MODE=MySQL;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

jwt:
  secret: test-secret-key-for-jwt-signing-minimum-256-bits-long-please
  access-token-validity-seconds: 3600
